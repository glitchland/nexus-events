[package]
name = "nexus-events-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
ctor = "0.4.0"

[dev-dependencies]
nexus-events = { path = ".." }